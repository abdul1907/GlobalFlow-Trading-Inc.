{
  "name": "globalflow-trading-inc",
  "private": true,
  "scripts": {
    "build": "npm --prefix nextjs-site run build",
    "vercel-build": "npm --prefix nextjs-site run build"
  },
  "dependencies": {
    "next": "^15.5.14"
  }
}
